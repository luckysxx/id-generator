services:
  id-generator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: id-generator
    command: ["-port=50059", "-node=1"]
    restart: unless-stopped
    ports:
      - "50059:50059"
    volumes:
      - ./configs:/configs:ro
    env_file:
      - .env
    networks:
      - go-net
    healthcheck:
      test: ["CMD", "true"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s

networks:
  go-net:
    external: true
